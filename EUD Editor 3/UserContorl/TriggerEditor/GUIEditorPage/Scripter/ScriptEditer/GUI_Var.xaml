<UserControl x:Class="GUI_Var"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:EUD_Editor_3"
             mc:Ignorable="d" 
             MinWidth="200" MinHeight="100"
             MaxWidth="600" MaxHeight="400">
    
    <DockPanel x:Name="MainStackPanel" LastChildFill="False">
        <Border x:Name="ValueSelecter" DockPanel.Dock="Right" Width="250"/>
        <TextBox x:Name="vname" materialDesign:HintAssist.Hint="变量名" Style="{StaticResource MaterialDesignFloatingHintTextBox}" VerticalAlignment="Center" DockPanel.Dock="Top" TextChanged="vname_TextChanged"/>
        <ComboBox x:Name="VarType" materialDesign:HintAssist.Hint="类型" DockPanel.Dock="Top" Style="{StaticResource MaterialDesignFloatingHintComboBox}" SelectionChanged="VarType_SelectionChanged">
            <ComboBoxItem Content="变量"/>
            <ComboBoxItem Content="静态变量"/>
            <ComboBoxItem Content="常量"/>
            <ComboBoxItem Content="对象"/>
        </ComboBox>
        <CheckBox x:Name="initcb" Content="初始值" DockPanel.Dock="Top" Checked="initcb_Checked" Unchecked="initcb_Unchecked"/>

        <WrapPanel x:Name="EditValuesPanel" Visibility="Visible" DockPanel.Dock="Top">
            <Button Content="ㅁㄴㅇ"/> <!--临时占位符？-->
            <Button Content="ㅁㄴㅇ"/> <!--临时占位符？-->
            <Button Content="ㅁㄴㅇ"/> <!--临时占位符？-->
            <Button Content="ㅁㄴㅇ"/> <!--临时占位符？-->
        </WrapPanel>
        <DockPanel x:Name="varObjectBorder" DockPanel.Dock="Top" >
            <!-- 必须选择对象. -->
            <!-- 从选定的对象中选择函数。 -->
            <DockPanel DockPanel.Dock="Top">
                <CheckBox x:Name="SpFlag" Content="游戏中初始化" DockPanel.Dock="Right" Checked="SpFlag_Checked" Unchecked="SpFlag_Unchecked" Visibility="Collapsed"/>
                <ComboBox x:Name="ObjectFunc" materialDesign:HintAssist.Hint="函数" DockPanel.Dock="Left" Style="{StaticResource MaterialDesignFloatingHintComboBox}" SelectionChanged="ObjectFunc_SelectionChanged">
                    <ComboBoxItem Content="构造函数" Tag="constructor"/>
                    <ComboBoxItem Content="Cast" Tag="cast"/>
                    <ComboBoxItem Content="Alloc" Tag="alloc"/>
                </ComboBox>
            </DockPanel>
        
            <local:GUI_VarFuncUse x:Name="varFunction"/>
        </DockPanel>
        
        <!-- <CheckBox x:Name="initcb" Content="使用初始值" DockPanel.Dock="Top" Checked="initcb_Checked" Unchecked="initcb_Unchecked"/> -->
        <Separator DockPanel.Dock="Top" />
        <local:GUI_ArgEditor x:Name="valueEditPanel" DockPanel.Dock="Top" Margin="5"/>
     
    </DockPanel>
</UserControl>
